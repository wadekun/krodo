# Architecture Review：`docs/architecture.md` v0.4

> Reviewer: AI Architect | Date: 2026-05-06 | Document Version: v0.4

---

## 总体评价

**v0.4 是一份完整、一致、可直接指导实现的设计基线文档。**

v0.3 Review 提出的 3 个一致性问题（A1-A3）和 3 个细化建议（B1-B3）全部修正。经过四轮迭代（v0.1 → v0.4），文档从 8.0 分稳步提升到当前水平，所有重大设计决策均有理有据，所有跨节引用保持一致。

---

## v0.3 Review 验收

| Review 项 | 修正内容 | 验收 |
|-----------|---------|------|
| A1 §0.2 monorepo 矛盾 | 改为 "`src/` layout 单包组织（详见 §6.1）" | **通过** |
| A2 §8 `packages/` 残留 | 1.3/1.4/1.6 全部改为 `coda/llm`、`coda/core`、`coda/sandbox` | **通过** |
| A3 §1 子系统计数 9→10 | 改为"10 个子系统" | **通过** |
| B1 prompt_version 事件类型 | 新增 `SESSION_INIT` 事件类型，prompt_hash 写入其中 | **通过** |
| B2 import-linter 归属 | 改为"独立工具 `import-linter`"，注明 ruff 不提供此能力 | **通过** |
| B3 RecoveryCoordinator | 降级为 `coda/core/recovery.py` 模块，不升格为 Protocol | **通过** |

**6/6 全部通过。**

---

## 跨节一致性扫描

对全文做了一次系统性的跨节交叉验证：

| 检查项 | §来源 | §目标 | 结果 |
|--------|-------|-------|------|
| 目录结构 | §0.2 `src/` layout | §6.1 `src/coda/...` | **一致** |
| 目录结构 | §6.1 `src/coda/...` | §12 `mkdir -p src/coda/{...}` | **一致** |
| 任务路径 | §8 1.3 `coda/llm` | §6.1 `src/coda/llm/` | **一致** |
| 任务路径 | §8 1.4 `coda/core` | §6.1 `src/coda/core/` | **一致** |
| 任务路径 | §8 1.6 `coda/sandbox` | §6.1 `src/coda/sandbox/` | **一致** |
| 工具数量 | §5.1 "11 个" | §5.2 列出 11 个 | **一致** |
| 工具数量 | §5.1 "11 个" | §9 "11 个核心工具（fs 5 + 搜索 1 + shell 1 + patch 1 + git 3）" | **一致** |
| 子系统计数 | §1 "10 个子系统" | §1 列出 10 条 | **一致** |
| tool call 上限 | §0.3 上限 15 / warning 10 | §12 配置 `max_tool_calls_per_turn: 15` / `soft_warning_at: 10` | **一致** |
| Python 版本 | §0.2 "3.12+" | §3.1 "Python 3.12+" | §12 `--python 3.12` | **一致** |
| prompt 版本 | §5.5.3 `SESSION_INIT` | §3.4 `SessionEventType.SESSION_INIT` | **一致** |
| error recovery | §7.5 `recovery.py` | §8 任务 1.4 "含 recovery.py 错误恢复模块" | **一致** |
| recovery 定位 | §7.5 不升格为 Protocol | §3.4 Protocol 列表中无 RecoveryCoordinator | **一致** |
| AGENTS.md 限长 | §5.6.2 单文件 8K / 总计 12K | §3.4.1 "超过 8K 截断" | **一致** |
| 依赖版本 | §12 `uv add` 版本约束 | §11 原则 7 "核心依赖锁定主版本" | **一致** |

**15/15 全部一致，未发现跨节矛盾。**

---

## 评分

| 维度 | v0.1 | v0.2 | v0.3 | v0.4 | 变化 |
|------|------|------|------|------|------|
| 完整性 | 8.0 | 8.5 | 9.5 | **9.5** | 稳定 |
| 正确性 | 7.0 | 8.0 | 9.0 | **9.5** | +所有 stale reference 已清理 |
| 可执行性 | 8.0 | 9.0 | 9.5 | **9.5** | 稳定 |
| 一致性 | 9.0 | 7.0 | 8.5 | **9.5** | +15 项跨节检查全通过 |
| 可维护性 | 8.0 | 8.5 | 9.0 | **9.5** | +变更日志精确到条目 |
| **综合** | **8.0** | **8.2** | **9.1** | **9.5** | |

---

## 结论

**v0.4 通过 Review，无阻塞项。可以立即启动 Phase 0。**

经过四轮 Review（v0.1 → v0.4），文档已收敛到高质量状态。所有关键设计决策有据可查，所有跨节引用保持一致，所有 Protocol 接口可直接指导实现，错误恢复矩阵和 token 预算算法等关键细节已充分展开。

建议的 Review 流程优化：正如开发同学提出的，将"每次重大修订后跑一次跨节一致性检查"固化为 PR checklist 条目——v0.2→v0.3 的 A1-A3 问题正是缺少这道检查的后果，v0.3→v0.4 的修正已经证明了这套流程的价值。
